apiVersion: v1
kind: Service
metadata:
  name: {{ include "do2602-chart.fullname" . }}
  labels:
    {{- include "do2602-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "do2602-chart.selectorLabels" . | nindent 4 }}
